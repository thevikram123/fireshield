name: fire_audit_demo
description: EY FireShield AI — Fire Safety Compliance & Audit Platform. Demo v1.0.0-demo
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^13.2.0
  fl_chart: ^0.67.0
  percent_indicator: ^4.2.3
  badges: ^3.1.2
  timeline_tile: ^2.0.0
  intl: ^0.19.0
  shimmer: ^3.0.0
  shared_preferences: ^2.2.2
  qr_flutter: ^4.1.0
  image_picker: ^1.1.2
  geolocator: ^13.0.0
  http: ^1.2.0
  supabase_flutter: ^2.17.2
  file_picker: ^11.0.2
  http_parser: ^4.1.2
  flutter_markdown: ^0.7.7+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
